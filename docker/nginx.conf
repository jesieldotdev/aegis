server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # service worker: nunca cachear, senão updates ficam presos
    location = /sw.js {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    location = /manifest.webmanifest {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    # assets com hash no nome podem ser cacheados por muito tempo
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    location / {
        # $uri/ serve páginas estáticas em pastas (ex.: /privacidade, /termos);
        # o restante cai no index.html (SPA).
        try_files $uri $uri/ /index.html;
    }
}
